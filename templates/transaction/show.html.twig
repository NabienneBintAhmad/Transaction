{% extends 'base.html.twig' %}

{% block title %}Transaction{% endblock %}

{% block body %}
    <h1>Transaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transaction.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ transaction.date ? transaction.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ transaction.montant }}</td>
            </tr>
            <tr>
                <th>Commission</th>
                <td>{{ transaction.commission }}</td>
            </tr>
            <tr>
                <th>EnvoyeurNomComplet</th>
                <td>{{ transaction.envoyeurNomComplet }}</td>
            </tr>
            <tr>
                <th>EnvoyeurCni</th>
                <td>{{ transaction.envoyeurCni }}</td>
            </tr>
            <tr>
                <th>RecepteurNomComplet</th>
                <td>{{ transaction.recepteurNomComplet }}</td>
            </tr>
            <tr>
                <th>RecepteurCni</th>
                <td>{{ transaction.recepteurCni }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('transaction_index') }}">back to list</a>

    <a href="{{ path('transaction_edit', {'id': transaction.id}) }}">edit</a>

    {{ include('transaction/_delete_form.html.twig') }}
{% endblock %}
