{% extends 'base.html.twig' %}

{% block title %}UserPrestataire{% endblock %}

{% block body %}
    <h1>UserPrestataire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_prestataire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user_prestataire.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user_prestataire.prenom }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ user_prestataire.contact }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_prestataire.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user_prestataire.Adresse }}</td>
            </tr>
            <tr>
                <th>Cni</th>
                <td>{{ user_prestataire.cni }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_prestataire_index') }}">back to list</a>

    <a href="{{ path('user_prestataire_edit', {'id': user_prestataire.id}) }}">edit</a>

    {{ include('user_prestataire/_delete_form.html.twig') }}
{% endblock %}
