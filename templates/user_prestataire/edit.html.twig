{% extends 'base.html.twig' %}

{% block title %}Edit UserPrestataire{% endblock %}

{% block body %}
    <h1>Edit UserPrestataire</h1>

    {{ include('user_prestataire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_prestataire_index') }}">back to list</a>

    {{ include('user_prestataire/_delete_form.html.twig') }}
{% endblock %}
